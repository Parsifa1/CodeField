#include <bits/stdc++.h>
using namespace std;
// #define int long long

void solve() {
    int m ,k, a1, ak;
    cin >> m >> k >> a1 >> ak;
    if(a1 >= m) {
        cout << 0 << "\n";
        return;
    }
    
}


signed main() {
    ios::sync_with_stdio(false);
    cin.tie(0), cout.tie(0);
    int T; cin >> T;
    while (T--) solve();
}